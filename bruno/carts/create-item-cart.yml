info:
  name: create-item-cart
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/cart/items"
  body:
    type: json
    data: |-
      {
        "productId": 5,
        "quantity": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
