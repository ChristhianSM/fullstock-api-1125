info:
  name: create-item-cart
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/cart/items"
  body:
    type: json
    data: |-
      {
        "productId": 50,
        "quantity": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
