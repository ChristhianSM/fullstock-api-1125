info:
  name: create-item-cart
  type: http
  seq: 5

http:
  method: post
  url: "{{base_url}}/api/cart/items"
  body:
    type: json
    data: |-
      {
        "productId": 7,
        "quantity": 4
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
