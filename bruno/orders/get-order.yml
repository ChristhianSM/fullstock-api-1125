info:
  name: get-order
  type: http
  seq: 3

http:
  method: get
  url: "{{base_url}}/api/order/:id"
  params:
    - name: id
      value: "2"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
