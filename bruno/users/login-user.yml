info:
  name: login-user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/sessions"
  body:
    type: json
    data: |-
      {
        "email": "christhian2524@gmail.com",
        "password": "1234561"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
