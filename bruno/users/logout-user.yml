info:
  name: logout-user
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/api/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
