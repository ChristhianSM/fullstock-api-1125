info:
  name: get-products-by-category
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/categories/:slug/products?minPrice=asdasd&maxPrice=asdasd"
  params:
    - name: minPrice
      value: asdasd
      type: query
    - name: maxPrice
      value: asdasd
      type: query
    - name: slug
      value: polos
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
